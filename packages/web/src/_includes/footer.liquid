<footer class="size-font-sm" data-theme="dark">
  <div class="container">
    <div class="stack gap--1">
      <h2 class="size-font-xl">Thanks for reading!</h2>
      <p>&copy; {{ site.author.name }}, 2019–Present</p>
      <p>
      <a
        href="https://www.github.com/{{ site.repo.owner }}/{{ site.repo.name }}/commit/{{ buildInfo.hash }}"
        rel="noreferrer noopener"
        target="_blank">Last built</a> on <time datetime="{{ buildInfo.time.raw }}">{{ buildInfo.time.formatted }}</time>
      </p>
    </div>
    <div class="end stack gap--1">
      <p>Enjoy my content? Consider <a class="outline-offset" href="https://buymeacoffee.com/hovhadovah">donating</a>.</p>
      <ul class="links">
        {%- assign networks = socials | entries -%}
        {%- for network in networks -%}
        {%- assign id = network | first -%}
        {%- assign data = network | last -%}
        <li>
          <a href="{{ data.url }}" target="_blank" rel="noreferrer noopener" class="outline-offset">
            {{ data.name }}
          </a>
        </li>
        {%- endfor -%}
        <li>
          <a href="/feed.xml" class="outline-offset">
            RSS
          </a>
        </li>
      </ul>
      {% if isThemed %}
        {% include "themePicker.html" %}
      {% endif %}
    </div>
  </div>
</footer>
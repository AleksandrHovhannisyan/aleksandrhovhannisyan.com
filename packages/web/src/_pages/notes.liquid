---
layout: default
title: Notes
description: Reusable code snippets and other technical notes. Unless otherwise noted, no attribution is required.
permalink: /notes/
---

<article class="rhythm">
    <header class="hero stack gap--2">
        <h1>Notes</h1>
        <p class="size-font-lg">Reusable code snippets and other technical notes. Unless otherwise noted, no attribution is required.</p>
    </header>
    <div data-theme="light">
        <ul class="grid col-2 gap-2 note-board">
            {%- assign notes = collections.notes | sortByKey: "date", "DESC" -%}
            {% for note in notes %}
              <li class="note">
                <article>
                  <h3 class="post-preview-title">
                    <a class="card-link" href="{{ note.url }}">{{ note.data.title | toSmartQuotes }}</a>
                  </h3>
                  <p class="post-preview-description">{{ note.data.description | toSmartQuotes }}</p>
                </article>
              </li>
            {% endfor %}
        </ul>
    </div>
</article>

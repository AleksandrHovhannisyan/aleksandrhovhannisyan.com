---
layout: default
title: Notes
description: Notes to self and short tutorials.
permalink: /notes/
---

<article class="rhythm">
    <h1 class="prose-tight">Notes</h1>
    <p class="prose-tight text-wrap-balance size-font-lg">Notes to self and short tutorials. Unless otherwise stated, no attribution is required for code snippets.</p>
    <div data-theme="light">
        <ul class="grid col-2 gap-2 note-board">
            {%- assign notes = collections.notes | sortByKey: "date", "DESC" -%}
            {% for note in notes %}
              <li class="note">
                <article>
                  <h3 class="post-preview-title">
                    <a class="card-link" href="{{ note.url }}">{{ note.data.title | toSmartQuotes }}</a>
                  </h3>
                  <p class="post-preview-description">{{ note.data.description | toSmartQuotes }}</p>
                </article>
              </li>
            {% endfor %}
        </ul>
    </div>
</article>
